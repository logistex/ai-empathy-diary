# 이 파일을 복사해 `.env`로 만들고 실제 값을 채우세요. (.env는 커밋되지 않습니다)

# OpenRouter (LLM)
OPENROUTER_API_KEY=

# PostgreSQL (AWS)
DATABASE_URL=

# 구글 OAuth (Auth.js) — Google Cloud Console에서 발급
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Auth.js 세션 암호화 키 — `openssl rand -base64 33` 로 생성
AUTH_SECRET=
